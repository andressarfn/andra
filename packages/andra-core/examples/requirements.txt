# Dependencies for real-provider examples.
# These are NOT part of the andra-core library.
#
# Install with:
#   pip install -r examples/requirements.txt
#
# Or, inside the project venv:
#   poetry run pip install -r examples/requirements.txt

# GitHub Models API uses the OpenAI-compatible SDK
openai>=1.0
